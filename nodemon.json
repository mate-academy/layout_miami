{
  "ignore": ["build/*", "dist/*", "public/*", "node_modules/*"],
  "watch": ["package.json"],
  "delay": "5s",
  "exec": "powershell -NoProfile -Command \"npm start; if ($LASTEXITCODE -ne 0) { (Get-Item 'package.json').LastWriteTime = Get-Date }\""
}
